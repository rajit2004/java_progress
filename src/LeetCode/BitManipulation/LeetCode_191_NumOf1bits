class Solution {
    public int hammingWeight(int n) {
        int count = 0;

        while (n != 0) {
            // Check if last bit is 1
            if ((n & 1) == 1) {
                count++;
            }

            // Unsigned right shift
            n = n >>> 1;
        }

        return count;
    }
}
